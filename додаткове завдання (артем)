#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <math.h>

int main()
{
    int A[12];
    srand(time(NULL));
    printf("\nСгенерований одновимірний масив\n");
    for(int i=0;i<12;i++){
        A[i]=100-rand()%100;
        printf("A[%i]=%i\n", i, A[i]);
    }
    return 0;
}
